# JaneStreet
Jane street kaggle competition

## Getting started

This repository is set up to be run in a linux shell terminal using python 3.7.
To initialise the modules in this repository, execute

```bash
source setup.sh
```

The code is stored primarily in the src/ directory. 
Utils contains the utilities (load data, that sort of thing), Models contains the models, Run contains the executable scripts
